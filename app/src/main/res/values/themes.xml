<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Moringa" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ash</item>
        <item name="colorPrimaryVariant">@color/ash</item>
        <item name="colorOnPrimary">@color/coal</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ash</item>
        <item name="colorSecondaryVariant">@color/ash</item>
        <item name="colorOnSecondary">@color/gold</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <!-- Dashboard Cards Style -->
    <style name="dash">

        <!-- Colors -->
        <item name="cardBackgroundColor">@color/ash</item>
        <item name="cardCornerRadius">15dp</item>
        <item name="android:textColor">@color/coal</item>
        <item name="fontFamily">@font/gilroy_semi</item>

    </style>

    <style name="deuce">

        <!-- Colors -->
        <item name="cardBackgroundColor">@color/coal</item>
        <item name="cardCornerRadius">15dp</item>
        <item name="android:textColor">@color/ash</item>
        <item name="fontFamily">@font/gilroy_semi</item>

    </style>

    <style name="duke">

        <!-- Colors -->
        <item name="android:tint">@color/coal</item>
        <item name="android:textColor">@color/churn</item>

    </style>

    <style name="coal">

        <!-- Colors -->
        <item name="cardBackgroundColor">@color/ash</item>
        <item name="cardCornerRadius">15dp</item>

    </style>

    <style name="ripah">

        <!-- Colors -->
        <item name="cardCornerRadius">15dp</item>

    </style>

</resources>